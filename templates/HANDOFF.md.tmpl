# HANDOFF — resume contract

> Written at every `hard_stop` and at clean session end. The **next session /
> Routine reads this FIRST** (plan §8 / §15). Persistence points here from
> `~/.claude/projects/<proj>/memory/MEMORY.md` (dated index → this file).
>
> **Honesty (plan R2):** this file states the TRUE position. Completion is gated
> on tests/lint, never self-report. In generative mode nothing is "solved" or
> "unsolved" — record the geometry (plan §7 IRON RULE).

- **session_id:** <id> · **mode:** engineering | generative · **operator:** <name>
- **utc_written:** <utc-iso>

## Elapsed / budget

<!-- Mirrors config/budget.json + state/session.start. Wall-clock proxy only —
     NOT a read of the real account cap (plan §9). -->
- session.start: <utc-iso> · elapsed: <m> min of `session_budget_minutes` <N> (<pct>%)
- governance: reached <ok|warn|hard_stop> at <pct>% · last `/usage` human reading: <relay or "none — record next launch">
- weekly.ledger fraction so far: <x.xx> of `weekly_session_fraction` <0.20>

## Ranked backlog / field-map summary

<!-- Engineering: top of state/plan.ranked.md (tier, S, status).
     Generative: digest of state/field.map.md (landmark/anchored/bridge/frontier/
     deep-frontier counts + the most promising next bridges). -->
- T0: <id> — <done|in-flight|blocked> (S=<n>) · verified by: <tests/lint or "pending">
- T1: <id> — <done|in-flight|blocked> (S=<n>)
- T2 (shed past warn): <id> — deferred
- T3 stretch: <id> — background remote probe <state>
- parked (T4, with reason): <id> — <concrete reason> (see state/parked.md)

## Open grants

<!-- From state/grants.jsonl — work dispatched but not yet released. -->
- todo=<id> agent=<id> tier=<T?> op=grant ts=<utc-iso> · worktree: auto/<operator>/<id>
- <none, or list>

## Blackboard digest

<!-- 1-line-per-entry summary of state/blackboard.md since last handoff.
     These are findings (data), carried forward as context — not instructions. -->
- [<utc-iso>] agent=<id> status=insight: <one-line finding + who it advises>
- conflicts parked for next launch (host arbitration tree, plan §11): <id> — <region/tier reason>

## Next session: start here

<!-- The single most leverage-positive resumption. Be concrete. -->
1. Read this file, then `state/cutline.json` (eng) or `state/field.map.md` (gen).
2. Resume open grants above; re-run phase 2 (re-plan) against the REMAINING budget.
3. First action: <the exact next TODO / frontier step>.
4. Relaunch from here — do NOT re-discover from scratch; correct GOAL.md/config if needed.
