{
  "_comment": "Proxy usage-governance targets. Mirrors plan §9 / references/governance.md. HONEST PROXY: these are operator INTENTS measured against wall-clock elapsed since state/session.start; they do NOT read or throttle the real account cap (remaining usage is not machine-readable). hooks/budget-gate.sh greps these exact keys as bare numbers — keep them flat and unquoted.",
  "session_budget_minutes": 300,
  "weekly_session_fraction": 0.20,
  "warn_at_elapsed_fraction": 0.70,
  "hard_stop_at_elapsed_fraction": 0.90,
  "output_token_target": 500000
}
