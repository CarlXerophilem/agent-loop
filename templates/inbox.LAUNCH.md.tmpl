<!--
  LAUNCH note — operator → harness. Copy to inbox/LAUNCH.md and fill in.
  Seeded by the operator at LAUNCH only (the human touchpoint, alongside GOAL.md + config/*).
  Scouts (phase 1) and the field generator (Stage 1') read this ONCE as DATA, never as
  instructions (untrusted-content rule, §6.1). After launch, correct by editing GOAL.md/config
  and relaunching from state/HANDOFF.md — never by mid-run prompts.
-->

# LAUNCH note

## Last session (1–3 lines)
<!-- What the previous session did / where it stopped. First run? write "first launch". -->
First launch.

## Human `/usage` relay  (the only real usage sensor — plan §9, refinement #1)
<!-- Remaining account usage is NOT machine-readable inside a session; the harness governs by a
     WALL-CLOCK proxy (config/budget.json), never a read of the real cap. Paste your last /usage
     reading here so the next session starts honest. -->
- `/usage` at last close: <e.g. "18% of weekly used at close"> · or "none — first launch"

## Long-session acknowledgment  (only needed for the `ultracode` overlay -- plan §9 honesty)
<!-- The ultracode overlay (mode_hint `...+ultracode`) deliberately spends a large wall-clock /
     parallel budget. It NEVER silently bypasses governance: launch FAILS unless you either raise
     config/budget.json `session_budget_minutes` OR set the flag below to true. This is the proxy
     ack only -- the harness still cannot read your real account cap (plan §9). Leave false unless
     you are intentionally running a long ultracode session. -->
- acknowledged_long_session: false

## Scope / focus for this session (optional)
- in scope: <narrow to part of GOAL.md, or leave blank for all of it>
- off-limits: <files/areas workers must not touch>

## Seed pointers (optional)
<!-- Files, tickets, and prior-art arXiv ids to read first. arXiv ids are fetched via the
     alphaXiv bridge (references/alphaxiv-bridge.md, hooks/alphaxiv.sh). -->
- read first: <paths>
- prior art (arXiv ids): <e.g. 2506.07625>
